# 🚌 RSS Media Bus v3.1 - Централизованная RSS шина

**Стабильная система мониторинга RSS с исправленными временными зонами**

## 🎯 Ключевые преимущества v3.1

- **🚌 Централизованная шина:** Один RSS Bus Core для всех пользователей
- **👥 Множественные пользователи:** Независимые уведомления каждому пользователю
- **🔄 Непрерывная работа:** 24/7 парсинг независимо от пользователей
- **⚡ Умный запуск:** Скрипт `start_system.sh` исключает дублирование процессов
- **🕐 Корректные временные зоны:** Исправлены проблемы UTC/Moscow конвертации
- **🚫 Без спама:** Отправляются только новые статьи после запуска
- **🛡️ Отказоустойчивость:** Автоматический перезапуск и мониторинг

## 🏗️ Архитектура системы

### 🚌 **RSS Bus Core** (Независимый сервис)
```
📡 RSS Sources → 🚌 RSS Bus Core → 🗄️ SQLite Database
```

- **Назначение:** Только парсинг RSS и сохранение в БД
- **Режим работы:** 24/7, **независимо от пользователей**
- **Интервал:** Парсит 46 источников каждые **5 минут**
- **Ответственность:** 
  - Мониторинг всех RSS источников
  - Извлечение полных данных статей (GUID, теги, медиа, метаданные)
  - Сохранение в единую SQLite базу данных с **UTC временными метками**
  - **Никаких уведомлений пользователям**

### 🔔 **User Notification Service** (Пользовательский сервис)
```
🗄️ Database → 🔔 User Service → 📱 Telegram Topics
```

- **Назначение:** Читает из БД и уведомляет пользователей
- **Режим работы:** Независимые фильтры для каждого пользователя
- **Интервал:** Проверяет новые статьи каждые **30 секунд**
- **Ответственность:**
  - Мониторинг новых статей в БД
  - Корректная конвертация временных зон Moscow ↔ UTC
  - Фильтрация по настройкам пользователя (46 источников → 49 топиков)
  - Отправка в Telegram топики с соблюдением rate limits
  - **Отправка только статей появившихся ПОСЛЕ запуска сервиса**

### 🎛️ **RSS Bus Manager** (Управляющий процесс)
```
🎛️ Manager → 🚌 RSS Core + 🔔 User Service
```

- **Назначение:** Запуск и мониторинг всех компонентов
- **Функции:**
  - Автоматический запуск обоих сервисов
  - Мониторинг здоровья процессов
  - Автоматический перезапуск при сбоях

## 🚀 Быстрый запуск

### ⚡ Рекомендуемый способ (исключает дублирование)

```bash
# Клонируем репозиторий
git clone <repository-url>
cd rss_media_bus

# Устанавливаем зависимости
pip install -r requirements.txt

# Запускаем систему умным скриптом
./start_system.sh
```

Скрипт `start_system.sh` автоматически:
- ✅ Останавливает все старые процессы
- ✅ Запускает RSS Bus Core (1 экземпляр)
- ✅ Запускает User Notification Service (1 экземпляр)
- ✅ Проверяет отсутствие дублирования
- ✅ Выводит PID процессов и пути к логам

### 📊 Проверка статуса

```bash
# Проверить запущенные процессы
ps aux | grep -E "(rss_bus_core|user_notification)"

# Мониторинг логов
tail -f rss_core.log        # RSS парсинг
tail -f user_service.log    # Отправка уведомлений

# Остановка системы
pkill -f 'rss_bus_core.py|user_notification_service.py'
```

## ⚙️ Конфигурация пользователя

### Структура конфигурации

```yaml
# config/users.yaml
all_sources_rss_user:
  telegram:
    chat_id: -1002883915655
    bot_token: "your_bot_token"
  sources:
    - tass.ru
    - ria.ru
    - rbc.ru
    # ... еще 43 источника
  processors: []
```

### Маппинг источников в топики

```json
// config/topics_mapping.json
{
  "tass.ru": 1,
  "ria.ru": 2,
  "rbc.ru": 3,
  // ... маппинг для всех 46 источников → 49 топиков
}
```

## 🔧 Исправленные проблемы v3.1

### ✅ Временные зоны
- **Проблема:** User Service неправильно сравнивал Moscow время с UTC в базе
- **Решение:** Добавлена корректная конвертация `pytz.timezone('Europe/Moscow')` → UTC
- **Результат:** Все новые статьи теперь находятся и отправляются

### ✅ Спам старыми новостями
- **Проблема:** При перезапуске отправлялись сотни старых статей
- **Решение:** `last_check = datetime.now()` вместо `datetime.now() - 3 hours`
- **Результат:** Отправляются только статьи появившиеся ПОСЛЕ запуска

### ✅ Дублирование процессов
- **Проблема:** Несколько RSS Core/User Service работали параллельно
- **Решение:** Скрипт `start_system.sh` с проверкой единственности процессов
- **Результат:** Каждая новость приходит ровно один раз

### ✅ "Зависание" RSS Core
- **Проблема:** RSS Core переставал парсить новые статьи
- **Решение:** Перезапуск с мониторингом и логированием
- **Результат:** Непрерывный парсинг RSS источников

## 📊 Текущий статус системы

### Источники данных (46 активных)
- **Новостные:** TASS, РИА, RBC, Лента.ру, Известия
- **Международные:** Reuters, AP News, BBC
- **Технические:** Electrek, TechCrunch
- **Научные:** Mongabay, Happy Eco News
- **Финансовые:** Vedomosti, Bloomberg

### Пользователи (1 активный)
- **all_sources_rss_user:** 46 источников → 49 топиков Telegram

### Производительность
- **RSS парсинг:** Каждые 5 минут
- **Уведомления:** Каждые 30 секунд  
- **Rate limit:** ~1 сообщение в минуту (Telegram API)
- **База данных:** SQLite с UTC временными метками

## 🛠️ Диагностика и мониторинг

### Проверка работы системы

```bash
# 1. Процессы запущены
ps aux | grep python3 | grep -E "(rss_bus_core|user_notification)"

# 2. Новые статьи добавляются
sqlite3 rss_media_bus.db "SELECT datetime(added_date, 'localtime'), feed_id, title FROM articles ORDER BY added_date DESC LIMIT 5;"

# 3. Нет дублирования статей
sqlite3 rss_media_bus.db "SELECT title, COUNT(*) FROM articles WHERE added_date > datetime('now', '-1 hour') GROUP BY title HAVING COUNT(*) > 1;"

# 4. User Service активен
tail -f user_service.log | grep "Отправлено статей"
```

### Скрипты мониторинга

```bash
# Добавление нового пользователя
python3 scripts/add_user.py

# Мониторинг системы
python3 scripts/system_monitor.py

# Просмотр пользователей
python3 scripts/list_users.py
```

## 🐛 Решение проблем

### RSS Core не добавляет статьи
```bash
# Проверить RSS источники вручную
curl -s "https://ria.ru/export/rss2/archive/index.xml" | head -20

# Перезапустить систему
./start_system.sh
```

### User Service не отправляет уведомления
```bash
# Проверить last_check время
python3 -c "
from user_notification_service import UserNotificationService
import asyncio
async def check():
    service = UserNotificationService()
    await service.initialize_database()
    await service.load_users()
    print('last_check:', service.last_check_time)
asyncio.run(check())
"

# Перезапустить если нужно
./start_system.sh
```

### Дублирование новостей
```bash
# Проверить количество процессов
ps aux | grep python3 | grep -E "(rss_bus_core|user_notification)" | wc -l
# Должно быть ровно 2

# Если больше - перезапустить
./start_system.sh
```

## 📚 Дополнительная документация

- **[QUICKSTART.md](QUICKSTART.md)** - Подробное руководство по запуску
- **[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)** - Детальная архитектура
- **[docs/SCALABILITY.md](docs/SCALABILITY.md)** - Масштабирование системы
- **[docs/PROCESSES.md](docs/PROCESSES.md)** - Управление процессами

## 🎯 Планы развития

- **Веб-интерфейс:** Управление пользователями через браузер
- **Метрики:** Grafana дашборд с производительностью
- **Фильтры:** Более гибкая настройка ключевых слов
- **Уведомления:** Email и другие каналы связи
- **API:** REST API для внешних интеграций

---

**📞 Поддержка:** Если система не работает, запустите `./start_system.sh` и проверьте логи `rss_core.log` и `user_service.log` 